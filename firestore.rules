rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Hackathon: open access. Backend uses service account (ADC) which bypasses
    // these rules anyway. Tighten before going to production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
